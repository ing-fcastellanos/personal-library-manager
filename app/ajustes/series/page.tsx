import { SeriesIndex } from "@/components/series/series-index";

/**
 * Series index (#38): every tracked series with its completion. Settings
 * sub-page, same shape as `/ajustes/qr` — not a bottom-nav entry.
 */
export default function SeriesIndexPage() {
  return <SeriesIndex />;
}
